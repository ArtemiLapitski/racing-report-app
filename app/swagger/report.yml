Example endpoint returning a racing report
This is using yaml for specifications.
---
 parameters:
    - in: query
      name: order
      schema:
        type: string
        enum:
          - desc
          - asc
      description: Order of the report

 responses:
   200:
     description: The report
